  #include <iostream>

using namespace std;

int main()
{
  int x, y, z, u, v;
  cin>>x>>y>>z>>u>>v;
  if(x<=u||y<=u||z<=u){
    if(x<=u&&(y<=v||z<=v)){cout<<"проходит"; return 0;}
    if(y<=u&&(x<=v||z<=v)){cout<<"проходит"; return 0;}
    if(z<=u&&(y<=v||x<=v)){cout<<"проходит"; return 0;}else{cout<<"не проходит"; return 0;}
  }
  return 0;
}
